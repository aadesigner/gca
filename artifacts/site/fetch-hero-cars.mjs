/** DISABLED — never download Unsplash / stock imagery for the marketing site.
 *  Car photos must come from DB listings via export-site-cars.mjs.
 */
console.error("fetch-hero-cars.mjs is disabled. Use export-site-cars.mjs (DB-only, no stock).");
process.exit(1);
